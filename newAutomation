import time
from selenium import webdriver
from selenium.webdriver.common.by import By
import tkinter as tk

window = tk.Tk()


driver = webdriver.Chrome()
driver.get("http://moodle.mitsgwalior.in/login/index.php")
# link = driver.find_element(By.LINK_TEXT,"Log in")
# link.click()

driver.find_element(By.ID , "username").send_keys("2021mc46ra")
driver.find_element(By.ID , "password").send_keys("Rxxggee@123")
driver.find_element(By.ID , "loginbtn").click()
# driver.find_element(By.ID, "label_3_15").click()
mycourse_cont = [driver.find_element(By.ID, "inst559980").text]
print(mycourse_cont)

# greet = tk.Label(text = mycourse_cont)
# greet.pack()

time.sleep(60)

time.sleep(5000)
